import { StyleSheet } from "react-native";

const styleConfiguracao = StyleSheet.create({
    viewConfig: {
        display:"flex",
        alignItems:"center",
        justifyContent:"center"
    },
    textArea: {
        display:"flex",
        flexDirection:"row"
        
    }
})

export default styleConfiguracao